{
  "name": "dos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startDB": "json-server --watch users.json",
    "server": "node server.js",
    "start": "concurrently \"npm run startDB\" \"npm run server\""
  },
  "author": "siboniso",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "json-server": "^0.17.0"
  }
}
